Usage
------------

First you need to install docker, you can take it by this command:
``` {.sourceCode .bash}
$ ./run.sh setup
```

And then build and run project
``` {.sourceCode .bash}
$ ./run.sh stack
```

Up/Down project
``` {.sourceCode .bash}
$ docker-compose up -d
$ docker-compose down
```